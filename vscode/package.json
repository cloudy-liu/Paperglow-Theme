{
  "name": "paperglow-markdown-preview",
  "displayName": "Paperglow Markdown Preview",
  "description": "A single-tab Paperglow Markdown preview with a compact Preview | Markdown switch.",
  "version": "0.1.0",
  "publisher": "cloudy-liu",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/cloudy-liu/Paperglow-Theme.git",
    "directory": "vscode"
  },
  "engines": {
    "vscode": "^1.89.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "activationEvents": [
    "onLanguage:markdown",
    "onCommand:paperglow.openPreview"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "paperglow.openPreview",
        "title": "Open Paperglow Preview",
        "category": "Paperglow",
        "icon": "$(open-preview)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "paperglow.openPreview",
          "when": "resourceLangId == markdown",
          "group": "navigation@1"
        }
      ]
    }
  },
  "scripts": {
    "build:bundle": "esbuild src/extension.ts --bundle --platform=node --target=node18 --format=cjs --external:vscode --outfile=out/extension.js",
    "compile": "tsc -p .",
    "test": "npm run compile && node --test \"out/test/*.test.js\"",
    "package:vsix": "npm run build:bundle && vsce package --out paperglow-markdown-preview.vsix"
  },
  "dependencies": {
    "markdown-it": "^14.1.0",
    "markdown-it-task-lists": "^2.1.1",
    "sanitize-html": "^2.17.3"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^20.12.12",
    "@types/sanitize-html": "^2.16.1",
    "@types/vscode": "^1.89.0",
    "@vscode/vsce": "^3.2.2",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.3"
  }
}
